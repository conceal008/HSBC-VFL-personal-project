# 数据卡 · 合成数据（SCM 生成）

- 产出日期：2026-09-04（S9.1）

## 基本信息

| 项 | 内容 |
|---|---|
| 类型 | **程序生成的合成数据**，不含任何真实个人信息 |
| 生成器 | `modules/m2_synthetic/components/scm_generator.py` |
| 配置 | `configs/scenarios.yaml`（8 场景）· `configs/scenarios_nonlinear.yaml`（6 场景） |
| 种子 | 11 / 22 / 33 / 44 / 55（关键对照另用 101–140） |
| 规模 | n_party_a 默认 40,000，冒烟 6,000 |

## 结构参数（13 个可控）

互补性 λ · 冗余度 · 重叠率 · 同意率与选择性 · 匹配错误率 · 时间漂移 ·
标签噪声 · 异质处理效应 · 边际漂移 · 规模不对称 · 正样本率 · 特征维数（共享/A/B）

## 信号形式（三种）

| 形式 | 信号 | 默认 |
|---|---|---|
| `linear` | s_shared + s_a + λ·s_b | ✅ |
| `interaction` | s_shared + s_a + λ·(s_a × s_b) | |
| `threshold` | s_shared + s_a + λ·1[s_b > 0] | |

**默认必须保持 linear**——既有全部结论建立在该形式上。

## 正确性验收

| 判据 | 结果 |
|---|---|
| λ=0 时理论增益为 0（三种形式） | ✅ 全部为 0.0000 |
| 正样本率校准到配置值 | ✅ 偏差 < 0.02 |
| 同种子完全可复现 | ✅ 逐位一致 |
| 两份配置口径一致（N6 vs S1_基准） | ✅ 差 < 1e-12 |

## 已知局限

- 单一参数化生成器族，未覆盖真实数据的重尾、缺失、类别型特征与时序结构
- 信号是潜变量的函数，真实数据的机制未知
- **只能验证机制，不能承诺真实业务效果**
