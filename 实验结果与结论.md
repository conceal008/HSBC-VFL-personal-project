# 纵向联邦学习价值验证 · 初版实验结果与结论

- **产出日期**：2026-09-03
- **仓库**：https://github.com/conceal008/HSBC-VFL-personal-project
- **对应模块**：M2 合成数据 / M3 实体对齐 / M5 建模 / M6 评估 / M7 安全
- **可复现入口**：`modules/*/notebooks/S*.ipynb`（均带执行输出）

> ## ⚠️ 结论的适用边界（先读这一段）
>
> 1. **本文全部数字来自合成数据**，不是真实业务数据。合成数据能验证**机制**
>    （「什么条件下联邦学习有价值」），**不能**用于承诺真实业务效果。
> 2. 算法为**自实现**（NumPy），不是 FATE / SecureBoost 等生产实现。
>    正确性通过「联邦解 ≈ 集中式解」的等价性检验，不代表工程性能。
> 3. **本项目不设合规角色**，文中涉及法规的判断均**未经法务复核**（豁免 W-001）。
> 4. 「隐私保护」相关结论限于本文明确列出的攻击设定，**不构成安全性证明**。

---

## 一、一句话结论

**纵向联邦学习在本项目场景下的商业前提成立，但成立得比预期窄；无防护的协议是双向完全泄露的，而有效防护必须逐攻击面设计。**

三个支撑：

| 编号 | 结论 | 关键数字 |
|---|---|---|
| C1 | 低合规成本的替代方案（L1）拿不到大头，VFL 的增量是真实的 | L1 只捕获 VFL 价值的 **12.3%** |
| C2 | 价值高度依赖「互补性」这一个条件，不是普遍成立 | λ=0 时增益 0.025，λ=2.0 时 0.209 |
| C3 | 逐轮加噪防不住标签推断；压住泄露所需的噪声会先把价值清零 | σ=30 时泄露降至 0.67，但可用性 0.635 < L0 基线 0.709 |
| C4 | **无防护的纵向联邦逻辑回归是双向完全泄露的**——主动方拿特征，被动方拿标签 | 6 个辅助样本即可精确恢复特征（R²=1.0000） |
| C5 | 防护必须逐攻击面设计：同一种加噪手段对两类攻击效果**完全相反** | 上行 σ=0.1 使特征 R² 1.00→0.20，可用性仅损失 0.0014 |
| C6 | 成员泄露由**模型容量**驱动，与是否联邦无关 | L0/L1/L4 均为 0.510；GBDT 深 3→6 为 0.528→0.545 |
| C7 | **模型效果排序是生成假设的产物，已按分支卡 falsifier 作废** | 强交互信号下 L3b 纵向 GBDT(0.8193) 反超 L3a 联邦 LR(0.7903) |
| C8 | C1 不依赖生成机制：三种信号形式下 L1 捕获比例均 ≤1% | L1−L0 在 6 个场景中 5 个为负 |
| C9 | **理论上限高 ≠ 可实现比例高**，用一个数字概括数据价值不可靠 | 同 λ=0.8，交互的可实现比例约为线性的 1/4 |
| **C10** | **上行加噪与残差合法性检查必须同时开启，任一单独存在时另一条即失效** | 只加噪：幅度 1000 时 100 次重复即攻破；只查合法性：伪装探针 R²=0.859 且 0 次标记 |
| C11 | 恶意被动方可**操纵名单**，且中等幅度不触发原有的稳定性告警 | 幅度 1.0 时 45% 目标进入 Top-10%，重合度 0.93 > 旧阈值 0.9 |
| **C12** | **下行加噪防不住标签推断，却防得住模型窃取**——一类只需符号，另一类需要数值 | σ=0 时名单被完整复现（重合 1.000）；σ=1.0 降到 0.126，可用性代价近零 |
| C13 | 多次建模的信息累积不构成实际威胁——**但完全依赖合法性检查在场** | 合法幅度下 6000 次重训仍失败；幅度不受限时 100 次即攻破 |

---

## 二、五级基线阶梯

「联邦学习能不能跑通」不是科学问题。真问题是：**相对于合规成本低得多的替代方案，它多带来多少价值。**

| 级别 | 含义 | 跨境暴露面 |
|---|---|---|
| L0 | 内地单方建模 | 无 |
| L1 | 加对方 k-匿名聚合统计 | 聚合量，非个人信息 |
| L2 | 加对方粗粒度标记 | 逐人低维标记 |
| L3 | 纵向联邦（LR / GBDT / SplitNN） | 中间量逐人交换 |
| L4 | 集中式（原始数据汇集） | 全量个人信息 |

**L1 是 VFL 真正的竞争者**——它几乎没有合规成本。若 L1 能拿到大部分价值，本项目的商业前提就不成立。

### 2.1 阶梯总表

8 场景 × 5 种子 × 随机划分，AUC 均值与跨种子 95% 置信区间：

| 级别 | AUC | 95% CI |
|---|---|---|
| L4 集中式 LR | 0.8003 | [0.7759, 0.8248] |
| **L3a 联邦 LR** | **0.7978** | [0.7724, 0.8232] |
| L3b 纵向 GBDT | 0.7697 | [0.7437, 0.7958] |
| L4 集中式 GBDT | 0.7697 | [0.7437, 0.7958] |
| L3c SplitNN 形态A 双向 | 0.7664 | [0.7345, 0.7982] |
| L3c SplitNN 形态B 自监督 | 0.7213 | [0.6954, 0.7472] |
| L2 粗粒度标记 | 0.7183 | [0.7001, 0.7364] |
| L0 内地单方 LR | 0.7127 | [0.6964, 0.7290] |
| **L1 k-匿名统计** | **0.7097** | [0.6929, 0.7265] |
| L3c SplitNN 形态B 随机 | 0.7076 | [0.6813, 0.7338] |
| L0 内地单方 GBDT | 0.6758 | [0.6540, 0.6975] |

三个要点：

1. **L3a 联邦 LR（0.7978）≈ L4 集中式 LR（0.8003）**，差 0.0025。
   这不是「效果好」，而是**实现正确性的验收**——数学上二者本就等价，
   若差距明显则说明实现有 bug。
2. **L3b 纵向 GBDT 与 L4 集中式 GBDT 完全相等（0.7697）**。
   直方图交换协议在数值上无损，这也是等价性检验。
3. **L1（0.7097）连 L0（0.7127）都没超过。** 见下节的机制解释。

### 2.2 L1 为什么几乎无效——附安慰剂对照

L1 的分段键**只能用主动方特征**（否则就需要逐人的对方信息，那已是 L2）。
于是回传的分段统计量是 `E[X_B | 主动方分段]`——它是主动方特征的函数，
信息量在很大程度上**已被主动方特征本身包含**。

为把「分段带来的模型灵活性」与「B 侧数据的真实贡献」分开，设置**安慰剂臂**：
把 B 侧换成同形状纯噪声，其余流程完全相同。

关键对照单独用 **40 个种子**（其余实验为 5 种子）以压窄置信区间：

| 对照 | 差值 | 95% CI | 显著性 |
|---|---|---|---|
| L1 − L0（L1 的表观增益） | +0.0041 | [+0.0004, +0.0078] | 显著 |
| **L1 − 安慰剂（B 的净贡献）** | **+0.0078** | [+0.0039, +0.0116] | 显著 |
| L3a − L0（VFL 总增益） | +0.0631 | [+0.0537, +0.0726] | 显著 |
| **L3a − L1（VFL 净增量）** | **+0.0590** | [+0.0501, +0.0680] | 显著 |

→ **L1 捕获了 VFL 价值的 12.3%。** B 侧数据经 k-匿名聚合后确实还剩一点信息，
但只有零头。**C1 成立。**

补充：k 值取 5 / 10 / 20 对 L1 的 AUC 几乎无影响（0.7068 / 0.7068 / 0.7069）。
L1 的瓶颈不在 k-匿名的强度，而在**分段键只能用主动方特征**这一结构性限制。

### 2.2b 稳健性检验：调参能否救回 L1？

前述结论都用**同一组固定超参**。一个合理的质疑是：C1 会不会只是「L1 欠调参」？

依 DR-GOV-009 的红线，**L1 的搜索空间刻意给到最大**——60 组，是 L0 的 12 倍。
若调参能翻转 C1，必须让它有机会翻转。方法上有一条硬要求：**选参只看验证集**
（60/20/20 三分）；在测试集上选参会让搜索空间大的级别虚高更多，污染方向恰好
**有利于 L1**，同样会毁掉结论。

3 场景 × 5 种子，各级别在**测试集**上的表现：

| 级别 | 网格点数 | 验证 AUC | 测试 AUC | 验证减测试 |
|---|---|---|---|---|
| L4 集中式 LR | 5 | 0.8460 | **0.8390** | 0.0070 |
| L3a 联邦 LR | 27 | 0.8466 | **0.8352** | 0.0114 |
| L3c SplitNN | 54 | 0.8451 | 0.8210 | 0.0241 |
| L3b 纵向 GBDT | 81 | 0.8366 | 0.7954 | 0.0412 |
| L2 粗粒度标记 | 5 | 0.6989 | 0.7254 | −0.0265 |
| **L1 k-匿名统计** | **60** | 0.7265 | **0.7148** | 0.0116 |
| L0 内地单方 | 5 | 0.6945 | 0.7122 | −0.0177 |

「验证减测试」随网格增大而增大（L3b 的 81 组差 0.0412）——选参乐观度被正确检出，
这是方法本身的自检。

**最强的一击：让 L1 直接在测试集上挑最优点。** 下面的 oracle 上界**不是有效估计**
（它用了测试集选参，等于作弊），但它给出「调参最多能帮 L1 到什么程度」的上限：

| 口径 | L1 − L0 | L3a − L0 | **L1 捕获比例** |
|---|---|---|---|
| 正规选参（验证集） | +0.0026 | +0.1230 | **2.1%** |
| oracle 上界（作弊） | +0.0193 | +0.1246 | **15.5%** |
| 前述 40 种子安慰剂对照 | +0.0078（净） | +0.0631 | **12.3%** |

**三个独立口径互相印证：调参救不回 L1。** 即便让它作弊，也拿不到 VFL 价值的六分之一。

L1 的瓶颈是**结构性**的，不是调参问题——分段键只能用主动方特征，
回传的统计量因而是主动方特征的函数。

### 2.2c 换一种数据生成机制：模型排序被推翻，C1 站住

上述全部场景的信号都是潜变量的**线性组合**。这留了个隐患：
「调参后 L3a 联邦LR > L3c SplitNN > L3b 纵向GBDT」这个排序，
有多少是模型本身的差别，有多少只是**生成方式偏向线性模型**？

分支卡 `BC-M5-001` 的 falsifier 正是为此写下。新增两种非线性信号形式：

| 形式 | 信号 | 检验的机制 |
|---|---|---|
| `linear` | s_shared + s_a + λ·s_b | 可加贡献（原场景库） |
| `interaction` | s_shared + s_a + λ·(s_a × s_b) | 被动方**只经交互**起作用 |
| `threshold` | s_shared + s_a + λ·1[s_b > 0] | 被动方以阶跃方式起作用 |

`interaction` 最有理论动机：同样的 s_b，在 s_a 高的人身上是正向、低的人身上是负向。
线性模型无论怎么调参都拿不到（需要显式交叉项），树模型可以逼近。
**「双方特征的交互只有联合建模才能捕获」正是纵向联邦的核心论点之一。**

各级别在各自网格上调参后的测试集 AUC（5 种子）：

| 级别 | N6 线性对照 | N1 交互 λ=0.8 | **N2 交互 λ=2.0** | N3 阶跃 λ=0.8 | N4 阶跃 λ=2.0 |
|---|---|---|---|---|---|
| L0 内地单方 | 0.7059 | 0.7303 | 0.7506 | 0.7171 | 0.6936 |
| L1 k-匿名统计 | 0.7056 | 0.7285 | 0.7483 | 0.7178 | 0.6882 |
| L3a 联邦 LR | **0.7814** | **0.7512** | 0.7903 | **0.7810** | **0.8066** |
| **L3b 纵向 GBDT** | 0.7416 | 0.7405 | **0.8193** | 0.7534 | 0.7926 |
| L3c SplitNN | 0.7651 | 0.7465 | 0.7966 | 0.7626 | 0.7843 |

#### 🔴 falsifier 触发：模型排序作废

**N2_交互_高（λ=2.0）下排序反转**：
`L3b 纵向 GBDT(0.8193) > L3c SplitNN(0.7966) > L3a 联邦 LR(0.7903)`。
N4_阶跃_高 下 L3b 也升到第二。而 λ=0.8 的弱非线性与线性对照下 L3a 仍居首。

按分支卡写下的条件，**`tuned_test_auc` 不得再作为路线取舍依据**。
它度量的是「在本 SCM 的生成假设下谁更契合」，不是「哪条路线更好」。

**这不是某条路线更差，而是模型族与数据生成机制的匹配问题。**
真实业务数据的机制未知，因此**任何单一合成数据上的效果排序都不可外推**。
三条 L3 路线的取舍改由**暴露面与合规成本**决定（§5 的攻防结果）；
效果维度只能给出「在已知机制下的条件性排序」。

#### C1 在三种机制下都成立，且更强

| 场景 | L1 − L0 | 最优 L3 − L0 | L1 捕获比例 |
|---|---|---|---|
| N6 线性对照 | −0.0003 | +0.0755 | −0.4% |
| N1 交互 λ=0.8 | −0.0018 | +0.0209 | −8.7% |
| N2 交互 λ=2.0 | −0.0023 | +0.0687 | −3.4% |
| N3 阶跃 λ=0.8 | +0.0007 | +0.0638 | 1.0% |
| N4 阶跃 λ=2.0 | −0.0053 | +0.1130 | −4.7% |

**L1 − L0 在 6 个场景中有 5 个为负。** 交互信号下 L1 尤其差——
分段统计量是主动方特征的函数，**捕获不了逐人的交互效应**。
C1 不依赖于生成机制。

#### 一个新发现：理论上限高 ≠ 可实现比例高

同样 λ=0.8，交互形式的**理论**增益（0.0378）高于线性（0.0360），
但**实测**最优 L3 增益只有 0.0209，远低于线性的 0.0755。
可实现比例（实测 ÷ 理论）在三种机制间相差近四倍。

→ **用一个数字概括「对方数据值多少」是不可靠的**：
同样的理论价值，在不同机制下能落袋的比例天差地别。
尽调阶段若只用线性模型估增益，可能严重偏离真实可实现值。

### 2.3 决策等价性：AUC 接近 ≠ 名单一致

业务上真正起作用的是**营销名单**，不是 AUC 小数点后第三位。

| 对照 | Top-10% 名单重合度 | 标准差 |
|---|---|---|
| L1 vs L3a 联邦 LR | 0.459 | 0.109 |
| L1 vs L3b 纵向 GBDT | 0.367 | 0.090 |
| L1 vs L3c SplitNN 形态A | 0.387 | 0.088 |

→ 改用 L1 会**换掉一半以上的目标客户**。即便 AUC 差距只有 0.06，
决策层面二者完全不等价。

---

## 三、条件价值曲线：VFL 在什么条件下有价值

「联邦学习有没有用」是个坏问题。可回答的是：**在什么条件下有用。**

以 S1_基准 为中心扫描四个结构参数（5 种子均值）：

### 3.1 互补性 λ —— 主导因子

| λ | L0 | L1 | L3a | L3a − L1 |
|---|---|---|---|---|
| 0.0 | 0.7215 | 0.7191 | 0.7469 | **0.0278** |
| 0.4 | 0.7230 | 0.7210 | 0.7584 | 0.0375 |
| 0.8 | 0.7089 | 0.7068 | 0.7868 | 0.0800 |
| 1.2 | 0.7075 | 0.7039 | 0.8305 | 0.1266 |
| 1.6 | 0.7053 | 0.7050 | 0.8823 | 0.1774 |
| 2.0 | 0.6922 | 0.6937 | 0.9011 | **0.2074** |

跨度八倍。**互补性是唯一真正的决定因素**——
如果对方数据讲的是同一件事，联邦学习不会创造信息。**C2 成立。**

### 3.2 零互补不等于零增益（一个反直觉发现）

设计 S2_零互补 时令 λ=0，理论增益应为 0。实测却有 +0.0254。

做控制实验分离原因：

| 设定 | 实测增益 |
|---|---|
| λ=0，冗余=0.3（S2 原样） | **+0.0254** |
| λ=0，冗余=0 | **−0.0057** |

→ 不是 bug。机制是：**冗余特征通过降低测量误差产生价值**。
B 侧持有 A 侧潜变量的另一份带噪观测，两份平均可抵消噪声。
λ=0 且冗余=0 时增益为负（−0.0057），那是多余特征的纯过拟合代价。

**结论**：评估对方数据价值时，不能只问「有没有我没有的信息」，
还要问「能不能帮我把已有的信息测得更准」。这两条是**不同的价值来源**。

### 3.3 重叠率与冗余度

| 重叠率 ρ | L3a − L1 | | 冗余度 | L3a − L1 |
|---|---|---|---|---|
| 0.05 | 0.1019 | | 0.0 | 0.0475 |
| 0.10 | 0.0762 | | 0.3 | 0.0800 |
| 0.20 | 0.0718 | | 0.6 | 0.0766 |
| 0.30 | 0.0800 | | 1.2 | 0.0685 |
| 0.50 | 0.0822 | | | |

冗余度呈**倒 U 形**：适度冗余最有利（去噪收益），过度冗余则挤占互补信息。

### 3.4 盈亏平衡：单人增益 ≠ 总价值

低重叠时**单人增益最高**（ρ=0.05 时 0.1019），但可触达人数极小。
决策价值 ≈ 增益 × 可触达人数 × 单人价值：

| 重叠率 | 单人增益 | 可触达人数（按百万基数） | 相对总价值 |
|---|---|---|---|
| 0.05 | 0.1019 | 50,000 | 5,095 |
| 0.10 | 0.0762 | 100,000 | 7,620 |
| 0.20 | 0.0718 | 200,000 | 14,360 |
| 0.30 | 0.0800 | 300,000 | 24,000 |
| 0.50 | 0.0822 | 500,000 | **41,100** |

→ **只看 AUC 增益会得出「重叠率越低越好」的错误结论。**
两者方向相反，总价值在中高重叠区最大。

---

## 四、实体对齐：不是主要风险

两类失败模式性质不同：**漏配**是样本少了，**错配**是把甲的特征接到乙身上。

| 漏配率 | 对齐召回 | | 错配率 | 增益剩余比例 |
|---|---|---|---|---|
| 0% | 1.000 | | 0% | 1.000 |
| 1% | 0.989 | | 5% | 0.924 |
| 3% | 0.969 | | 10% | 0.825 |
| 5% | 0.949 | | 20% | 0.614 |
| 10% | 0.899 | | 40% | 0.369 |

→ 漏配 10% 仅损失约 12% 增益；错配需达约 40% 才使增益折半。
**实体对齐质量不是本项目的主要风险**，主要风险在互补性与合规折损。

ECDH-PSI 成本量级（可行性判断，非性能基准）：
千万级对千万级约 610 MB 通信、2,000 万次模幂——**工程上不构成障碍**。

---

## 五、隐私攻击与防护（M7）

攻击者设定为**诚实但好奇的协议内参与方**，不假设外部窃听。

### 5.1 A1 标签推断：不加防护时标签完全泄露

被动方每轮收到残差 `r = p − y`。训练首轮权重为零、预测恒为 0.5，
于是 `r = 0.5 − y` 的**符号与标签一一对应**。

实测泄露 AUC = **1.0000**。**不加防护的纵向联邦逻辑回归，标签是完全泄露的。**

### 5.2 逐轮加高斯噪声——一个被自己推翻的结论

先看单轮攻击下的隐私—可用性权衡：

| σ | 单轮ε | 可用性 AUC | 泄露 AUC（单轮攻击） |
|---|---|---|---|
| 0 | ∞ | 0.7868 | 1.0000 |
| 0.3 | 16.15 | 0.7872 | 0.9878 |
| 1.0 | 4.84 | 0.7869 | **0.7574** |
| 3.0 | 1.61 | 0.7771 | 0.5933 |
| 10.0 | 0.48 | 0.7109 | 0.5291 |

看起来 σ=1.0 就能把泄露压到 0.76 而几乎不损失可用性——**这个结论太好了，必须证伪。**

攻击者只用了**单轮**残差。但噪声在轮间独立、标签恒定，**跨轮平均即可消掉噪声**：

| σ | 可用性 AUC | 单轮攻击 | **跨轮平均攻击** |
|---|---|---|---|
| 0 | 0.7868 | 1.0000 | 1.0000 |
| 0.3 | 0.7872 | 0.9878 | **1.0000** |
| 1.0 | 0.7869 | 0.7574 | **1.0000** |
| 3.0 | 0.7771 | 0.5933 | **0.9999** |
| 10.0 | 0.7109 | 0.5291 | 0.9037 |
| 30.0 | **0.6352** | 0.5149 | 0.6659 |

**证伪成立，5.2 开头的乐观读数被推翻：**

- σ=1.0 时跨轮平均攻击的泄露 AUC 回到 **1.0000**
- 要把泄露压到 0.67 需 σ=30，此时可用性 **0.6352 < L0 内地单方基线 0.7089**

→ **逐轮加高斯噪声不是本协议的有效防护。**
把泄露压下去所需的噪声，会先把联邦模型的价值清零——
防护到有效时，还不如不做联邦。**C3 成立。**

**可行路径只能是协议级手段**（安全聚合 / 同态加密 / 秘密分享），或改变暴露面本身。

> **降级说明**：本文的 ε 是**单轮**高斯机制预算（灵敏度取 1）。
> 未实现子采样放大与 RDP 会计，400 轮朴素合成后的总预算**不具备可解释的隐私含义**。
> 表中 ε 列仅用于标注噪声量级，**不可作为差分隐私保证引用**。

### 5.3 A2 / A3：「冻结编码器 = 更安全」是错的

| 协议形态 | 可用性 AUC | 嵌入反演 R²（均值） | 反演 R²（最大） | 梯度标签泄露 AUC |
|---|---|---|---|---|
| 形态A 双向（回传梯度） | 0.7690 | 0.4877 | 0.7497 | **1.0000** |
| 形态B 自监督（冻结） | 0.7402 | **0.6666** | **0.8745** | 0.5000 |
| 形态B 随机（冻结） | 0.7267 | 0.4964 | 0.7592 | 0.5000 |

两个反直觉结果：

1. **形态B 挡住了梯度标签泄露**（1.00 → 0.50）——不回传梯度，该暴露面直接消失。
2. **形态B 并没有降低特征反演风险，反而更糟**（自监督 R²=0.667 > 形态A 的 0.488）。
   PCA 编码器是线性的、更易求逆；随标签训练的编码器反而丢掉了更多与任务无关的信息。

→ **形态B 换掉的是标签暴露面，不是特征暴露面。**
选择形态时必须分别评估两类风险，不能用「不回传梯度所以更安全」一句话带过。

### 5.4 A4 特征推断：本项目最严重的单项发现

威胁模型：主动方每轮收到被动方上传的部分 logit `x_b · w_b`，并另有**少量样本**的
x_b 真值（辅助集）。这在现实中并不苛刻——双方共有的存量客户中，主动方可能已自行
采集过对方口径的字段。攻击分两步最小二乘：用辅助集解出每轮的 w_b，再用 w_b 反解
其余所有样本的 x_b。

| 辅助样本数 | 特征 R² 均值 | 最差维 |
|---|---|---|
| 4（< 特征维数 6） | 0.282 | −1.250 |
| **6（= 特征维数）** | **1.000000** | **1.000000** |
| 64 | 1.000000 | 1.000000 |

**只要辅助样本数达到特征维数，被动方的原始特征被精确恢复。**

加上 §5.1 的标签完全泄露：**无防护的纵向联邦逻辑回归是双向完全泄露的**——
主动方拿到被动方的特征，被动方拿到主动方的标签。
**「原始数据不出本地」在这个协议下不构成任何实质保护。**

### 5.5 上行加噪：与标签推断完全相反的结果

被动方对**上传的部分 logit** 加噪（`uplink_sigma`）。注意这与 §5.2 的下行加噪是
**不同参与方的自我保护**，防的是不同的攻击，不可互相替代。

| 上行 σ | 特征 R²（朴素） | 特征 R²（加强攻击） | 可用性 AUC | vs L0=0.709 |
|---|---|---|---|---|
| 0 | 1.0000 | 1.0000 | 0.7868 | +0.078 |
| 0.01 | 0.467 | 0.555 | 0.7870 | +0.078 |
| 0.1 | 0.196 | 0.280 | 0.7854 | +0.077 |
| 0.3 | 0.072 | ~0.2 | 0.7684 | +0.060 |
| 1.0 | 0.028 | 0.129 | 0.7205 | +0.012 |
| 3.0 | ~0.02 | ~0.1 | 0.6192 | **低于 L0** |

（可用性一列为**训练与推理均加噪**的口径。若只在训练时加噪、推理走干净通道，
AUC 几乎不变——但真实部署中推理同样要走协议，那个口径会低估代价。）

**结论与 §5.2 相反：上行加噪是有效防护，且在 σ ≤ 0.1 时代价可忽略**
（0.7868 → 0.7854，损失 0.0014，而泄露从 1.0000 降到 0.196）。

**机制解释**：A1 的信号（标签）**轮间恒定**，跨轮平均即可消噪；
A4 需要解一个**病态线性系统**（w_b 轨迹的条件数约 4.8×10⁴），噪声被放大五个数量级。

→ **同一种手段对两类攻击效果完全相反。防护方案必须逐攻击面评估，不能一刀切。**

**证伪检验**：加强版攻击（岭正则化 + 更大辅助集）确实优于朴素最小二乘，
但没有推翻结论——σ=1.0 时即使给攻击者 2000 个辅助样本（等于已知训练集大部分
x_b 真值，极其宽松的假设），R² 也只到 0.129。

另需注意：**朴素最小二乘在噪声下辅助样本越多反而越差**（σ=1.0、n_aux=2000 时
R² = −3.60），只报朴素版本会在相反方向上误导读者，故两版并列报告。

### 5.6 A5 成员推断：先验证攻击，再下结论

「这个人是否在你的建模样本里」本身就是个人信息，即便特征与标签都没泄露。

**朴素的损失阈值攻击没有通过有效性验证。** 在刻意制造记忆的模型上
（训练 AUC = 1.0000、过拟合间隙 0.32），它也只达到 0.52。攻击弱到这个程度，
**不能据此宣称「成员推断无威胁」**——那会重蹈 §5.2 的覆辙，只是方向相反。

因此改用 **LiRA 式影子模型校准**（32 个影子模型，逐样本比较「在训练集内」与
「不在训练集内」两种情形的置信度分布）。它随模型容量单调增强，通过了有效性验证。

| 级别 | LiRA 攻击 AUC | 标准差 |
|---|---|---|
| L0 内地单方 | 0.5105 | 0.0062 |
| L1 加 k-匿名统计 | 0.5105 | 0.0068 |
| L4 集中式 / L3 信息集 | 0.5103 | 0.0022 |
| L3b 纵向 GBDT 深 3 | 0.5277 | 0.0092 |
| **L3b 纵向 GBDT 深 6** | **0.5453** | 0.0070 |

**结论：成员泄露由模型容量驱动，与是否联邦无关。**
引入对方数据不增加成员泄露（0.5103 vs 0.5105），而树深从 3 到 6 使泄露上升 0.018。

→ **工程含义：控制树深，而不是纠结要不要联邦。**

> **降级说明**：本实现用 32 个影子模型，LiRA 原文通常用 64–256；且本项目样本量
> 约 2000、特征维数低，模型本身记忆有限。上述数值应读作**该设定下的下界**，
> 不构成「成员推断不可行」的一般结论。

### 5.7 恶意参与方：放开半诚实假设（S7.4）

此前 §5.1–5.6 的全部结论都建立在**半诚实**假设上——参与方严格执行协议，只是好奇。
这个假设在真实场景中往往不成立：**协议由代码执行，而代码由某一方控制**，
对方无从验证收到的数字是不是真按协议算出来的。

#### A6 恶意主动方：探针攻击

不发真实残差，改发构造的探针向量。被动方按协议更新 `w_b -= lr·(x_bᵀ·r)/n`，
若 r 是第 j 个样本上的单位向量，更新量正比于 `x_b[j]`——
下一轮上传量之差直接给出 `x_b·x_b[j]`。有 d 个辅助样本，
**每个目标只需 1 次探针**即可精确反解（无防护时 R² = 1.000000）。

与半诚实版本的差别在于：半诚实攻击只能利用梯度下降**自然形成**的 w_b 轨迹，
那条轨迹病态（条件数约 5×10⁴）因而对噪声敏感；
恶意攻击者**自己设计探针**，条件数由它掌控，还能放大幅度、重复取平均。

#### 🔴 C10 · 两项防护必须同时开启，缺一个另一个就失效

| 防护组合 | 攻破 σ=0.1 所需成本 |
|---|---|
| **只加噪**（幅度不受约束） | 幅度 1000 时**仅需 100 次重复** |
| **只查合法性**（无噪声） | 伪装探针 R²=0.859，**26 次全部未被标记** |
| **两者同时** | 合法幅度下需约 **10⁸ 次重复**，实际不可行 |

机制：单次探针的信号量级是 `lr·amplitude/n`，而噪声是固定的 σ——
**信噪比与幅度成正比**。约束幅度的唯一手段是残差合法性检查
（真实残差 `r = sigmoid(logit) − y` 必落在 [−1,1] 且稠密）。

→ **合法性检查把攻击成本抬高了六个数量级。它不是锦上添花，是让加噪防护有意义的前提。**
这条此前不在 M8 的防护基线里——**是本次实验补上的自身漏洞**。

#### C11 · 恶意被动方可操纵名单

被动方送的部分 logit 直接加进最终打分，主动方无从验证。实测（目标 2% 客户，Top-10% 名单）：

| 抬分幅度 | 目标进入率 | 名单重合度 |
|---|---|---|
| 0（不攻击） | 0.10 | 1.00 |
| **1.0** | **0.45** | **0.93** |
| 2.0 | 0.84 | 0.85 |
| 5.0 | 1.00 | 0.82 |

**旧的名单稳定性阈值 0.9 抓不到中等幅度的操纵**：幅度 1.0 时 45% 的目标客户被顶进名单，
而重合度仍有 0.93。M8 的基线已据此收紧到 **0.95**。

但要说清楚：这只是**检测**手段，不是防护——攻击者压低幅度仍可缓慢渗透。
根治需要主动方能验证被动方送来的部分 logit 确由 `x_b·w_b` 算出（零知识证明类手段），
本阶段未实现。

### 5.8 模型窃取与信息累积（S7.5）

#### 🔴 C12 · 被动方可独立复现整份名单

推导链条完全建立在已证的 A1 之上，不需要任何新假设：
A1 已证被动方能完全推断标签 y → 由 `r = sigmoid(logit) − y` 反解出 logit →
减去自己算得出的 `partial_b` → **得到主动方的全部贡献，逐样本**。

后果不是「偷到几个参数」，而是**被动方可以不依赖主动方独立出名单**。
这是商业资产的直接转移，也说明「原始特征没出域」不等于「资产没被拿走」。

| 下行 σ | 标签推断准确率 | **名单复现重合度** | 加强后（平均末 200 轮） |
|---|---|---|---|
| 0 | 1.0000 | **1.0000** | 0.991 |
| 1.0 | **1.0000** | 0.126 | 0.408 |
| 3.0 | 0.9994 | 0.104 | 0.181 |
| 10.0 | 0.9641 | 0.110 | 0.101 |

**这里出现了一个有用的不对称**：σ=1.0 时标签推断准确率仍是 **1.0000**（C3 得到印证），
但名单复现重合度已从 1.000 跌到 0.126（随机水平 0.10）。

机制清楚：
- **标签只需要残差的符号**——那是轮间恒定的一位信息，跨轮平均即可恢复；
- **模型窃取需要残差的精确数值**——它随训练变化不能跨轮平均，
  且信号量级只有 O(正样本率)=0.03，被 σ=1.0 的噪声完全淹没。

→ **不要因为「噪声防不住标签」就不加噪声。** 它保的是另一样东西：模型资产。
而 σ=1.0 的可用性代价几乎为零（0.7869 vs 无噪声 0.7868）。M8 基线已据此新增该项。

#### C13 · 多次建模的累积：不构成威胁，但前提很关键

生产系统本来就要周期性重训，**每次重训都给攻击者一批全新的独立观测**。
这与「重复探针」不同——重复探针会在流量上留下异常，而重训是协议要求的正常行为。

实测（合法幅度 + σ=0.1）：**6000 次重训（约每天重训 16 年）仍未攻破**。

**但这个「安全」完全依赖残差合法性检查在场**——幅度不受限时 100 次即可攻破。
对照实验已单独验证这一点，避免把「攻击太弱」误读为「防护有效」。

### 5.9 形态A vs 形态B 的代价

形态A（0.7664）比形态B 自监督（0.7213）高 **0.045 AUC**——
这是「不回传梯度」的效果代价。这个数字回答了 M0 阶段 S0.6 提出但当时无法回答的问题。

对照：**形态B 随机（0.7076）低于 L0 内地单方基线（0.7127）**。
随机冻结编码器不但没有价值，还不如不做联邦。**形态B 必须配自监督预训练才有意义。**

---

## 六、增量（uplift）评估

营销真正要的是「因为营销才转化」的人，**响应率 ≠ 增量**。

| 信息集 | AUUC | Uplift@10% |
|---|---|---|
| L0 内地单方 | 0.0059 | 0.0557 |
| L1 加 k-匿名统计 | 0.0058 | 0.0504 |
| L2 加粗粒度标记 | 0.0059 | 0.0616 |
| L4 全量（= L3 的信息集） | 0.0076 | **0.1066** |

→ **B 侧数据对增量的价值大于对响应率的价值**：
Top-10% 的增量近乎翻倍（0.0557 → 0.1066，+91%），而 AUUC 只涨 29%。
即 B 侧数据主要帮助**在高分段里把人挑对**，这正是营销预算实际花的地方。

---

## 七、划分口径：随机 vs 时间外推（OOT）

| 级别 | 随机划分 | OOT | 差值 |
|---|---|---|---|
| L3c 形态B 自监督 | 0.7213 | 0.7084 | 0.0129 |
| L3b 纵向 GBDT | 0.7697 | 0.7643 | 0.0054 |
| L0 内地单方 LR | 0.7127 | 0.7101 | 0.0026 |
| L3a 联邦 LR | 0.7978 | 0.8007 | −0.0029 |

差距很小（最大 0.013）。

> **不可外推的警示**：本合成数据的时间漂移强度是**人为设定**的。
> OOT 差距小**不代表**真实业务中也小。真实金融数据的分布漂移通常显著更强，
> 该结论**不得**外推到真实数据。此处只能得出一个方法层结论：
> 本实验的排序结论对划分口径不敏感，**不是**「OOT 无所谓」。

---

## 八、对项目决策的直接影响

| 编号 | 影响 | 依据 |
|---|---|---|
| I1 | VFL 的商业前提成立，可继续推进 | C1：L1 只拿到 12.3% |
| I2 | 立项前必须先估计**互补性**，这是唯一的决定性条件 | C2：跨度八倍 |
| I2b | 估互补性时还要问**它以什么形式起作用**（可加 / 交互 / 阶跃）——形式决定可实现比例 | C9 |
| I3 | 评估对方数据价值时要同时看「互补」与「去噪」两条来源 | §3.2 |
| I4 | 不得把「加噪声」当成通用隐私方案——**它防得住特征推断，防不住标签推断** | C3 + C5 |
| I4b | **被动方必须对上行加噪**（σ≈0.1），这是本项目发现的唯一低代价有效防护 | §5.5 |
| I4c | 标签侧仍须走协议级手段（安全聚合 / 同态加密 / 秘密分享），噪声无解 | C3 |
| I4d | 对外**不得**声称「原始数据不出本地即安全」——无防护协议双向完全泄露 | C4 |
| I4e | 控制树深以降低成员泄露；不必为此纠结是否联邦 | C6 |
| I5 | 形态A / 形态B 的选择要分别评估标签与特征两类暴露面 | §5.3 |
| I6 | 形态B 若采用，必须配自监督预训练，否则不如不做联邦 | §5.4 |
| I7 | 实体对齐可按常规工程标准做，不必过度投入 | §四 |
| I8 | 目标客群不应追求低重叠高纯度，总价值在中高重叠区 | §3.4 |
| I9 | **不得用合成数据上的效果排序选技术路线**——它随生成机制反转 | C7 |
| I10 | L3 三条路线的取舍改以**暴露面与合规成本**为主依据 | C7 + §5 |
| I11 | 尽调阶段不能只用线性模型估增益；须同时试树模型，并报告两者差距 | C9 |

---

## 九、已知缺口

以下为**本初版未覆盖**的部分，均为已知、未回避：

1. **无真实数据**：全部结论限于合成数据的机制层。
2. **无密码学实现**：PSI 为行为仿真；未实现同态加密 / 秘密分享 / 安全聚合。
3. **DP 无严格会计**：只有单轮 ε，无 RDP / 子采样放大。
4. **攻击面仍不完整**：已覆盖标签推断（A1）、嵌入反演（A2）、梯度标签推断（A3）、
   特征推断（A4）、成员推断（A5）、恶意主动方探针（A6）、恶意被动方抬分（A7）、
   **多次建模累积（A8）、模型窃取（A9）**；
   **仍未覆盖**多方合谋、模型窃取的黑盒查询变体、攻击者篡改自身模型结构、
   以及针对纵向 GBDT 的专门攻击（A4/A6 只针对联邦逻辑回归）。
5. ~~**未做超参搜索**~~ → **已完成**（§2.2b）：3 场景 × 5 种子，各级别按自身网格
   在验证集上选参、测试集上报告。**C1 未被翻转，反而更强**（正规口径 2.1%，
   oracle 上界 15.5%）。**仍未搜索**的是 M6 增量模型与 M7 攻击的超参。
6. ~~**单一 SCM 族**~~ → **已扩展**（§2.2c）：新增 interaction 与 threshold 两种信号形式。
   **结果：模型排序被推翻（C7），C1 被加固（C8）。** 仍只有一族参数化生成器，
   未覆盖真实数据可能有的重尾、缺失、类别型特征与时序结构。
7. **合规判断未经复核**：本项目不设合规角色（W-001）。

3'. **A5 的影子模型数偏少**：本实现用 32 个，LiRA 原文通常用 64–256。
   §5.6 的数值应读作该设定下的**下界**。

~~其中第 4 项（恶意参与方设定）是目前最可能改变结论的一项~~ → **已于 S7.4 补齐**，
且确实改变了结论：新增 C10（两项防护必须同时开启）与 C11（名单可被操纵），
并补上了 M8 防护基线自身的一个漏洞。

**S7.5 又补齐两类**：A8 多次建模的信息累积（最现实的一类「合谋」）、A9 模型窃取。
**仍未覆盖**：多方合谋、模型窃取的黑盒查询变体、攻击者篡改自身模型结构。

**已被消除的缺口（两轮）**：

- 「未覆盖特征推断与成员推断」（原第 4 项）→ 补齐后结论**实质变化**：
  新增 C4/C5/C6，其中 C4（双向完全泄露）与 C5（防护须逐攻击面设计）
  改变了对协议安全性的整体判断。
- 「未做超参搜索」（原第 5 项）→ 补齐后结论**未变，且更强**：
  C1 从 12.3% 收紧到 2.1%（正规口径），oracle 上界 15.5%。

一个改变了结论，一个加固了结论——**两种结果都只有做了才知道**。
这说明「已知缺口」不是免责声明，是待办清单。

---

## 九点五、工程与合规产出（M8 / M9）

本文前九节是**科学结论**。它们对项目的直接后果落在两处：

**M8 · 把结论固化为上线前拦得住的检查**（[`路线取舍决议`](modules/m8_industrialization/路线取舍决议.md) ·
[`上线准入清单`](modules/m8_industrialization/上线准入清单.md)）

- 六条防护基线，每条对应一个已实测的攻击，由 `guardrails.py` 机器核验
- 故障注入 **7/7 通过**（断点续跑 · 单方下线降级 · 网络抖动重试 ·
  schema 安全失败 · 批次一致 · 幂等 · 同意撤回）
- **硬结论：L3a 与 L3c 形态A 的标签暴露面目前无解，不具备上线条件**——
  不是效果不够，是暴露面无法闭合
- 上线准入清单列出 **5 项阻塞，当前全部未解除**

**M9 · 把结论固化为可追溯的证据链**（[`对外表述红线`](modules/m9_documentation/对外表述红线.md) ·
[`DPIA`](modules/m9_documentation/DPIA.md)）

- 一致性核验 13/13 · DPIA 风险溯源 7/7 · 交付清单 10/10，**全部机器核验**
- 已接入提交前流程：删掉任何被引用的结果文件都会被拦下
- **对外表述红线列出 9 条绝对不能说**，第一条即「原始数据不出本地所以安全」
- 剩余风险签署 **未签署且无人可签**——本项目不设合规角色

---

## 十、复现方式

```bash
git clone https://github.com/conceal008/HSBC-VFL-personal-project
cd HSBC-VFL-personal-project
pip install numpy pandas scikit-learn pyyaml matplotlib nbformat nbclient
python3 tools/notebooks_spec.py     # 重建并执行全部 notebook
bash ci/run_all_gates.sh            # 七道门禁 + 证据链核验 + 45 组回归用例
```

各 notebook 首个 cell 均打印 config 路径、随机种子与 git 版本，可核对运行环境。
