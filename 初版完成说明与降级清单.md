# 初版完成说明与降级清单

- **产出日期**：2026-09-03
- **依据豁免**：W-002（初版冲刺模式）
- **配套文件**：[实验结果与结论.md](实验结果与结论.md)

本文逐条说明：为完成初版，**哪些框架标准被降级、降到什么程度、为什么、代偿是什么、何时必须恢复**。
用户指令为「如果相关标准导致做不了的就进行标准降级，但要进行相关说明」——本文即该说明。

---

## 一、已完成的内容

| 模块 | 交付物 | 状态 |
|---|---|---|
| M2 合成数据 | SCM 生成器、8 场景配置、正确性验收 | 完成 |
| M3 实体对齐 | PSI 行为仿真、漏配/错配敏感性、成本量级 | 完成（仿真级） |
| M5 建模 | 五级阶梯 11 个模型、8 场景 × 5 种子 × 2 划分 | 完成 |
| M6 评估 | AUC/KS/Lift/Recall/Qini/AUUC/Top-K 重合、条件价值曲线 | 完成 |
| M7 安全 | A1 标签推断、A2 嵌入反演、A3 梯度推断、防护评估 | 完成（半诚实设定） |
| M4 特征工程 | **降级**，见 §2.4 | 最小实现 |
| M8/M9 | **降级**，见 §2.5 | 文档级 |

实现的算法与协议：

- 联邦逻辑回归（残差 ↔ 部分 logit 交换）
- 纵向 GBDT（SecureBoost 式 (g,h) 直方图交换）
- SplitNN 三形态（双向回传梯度 / 冻结随机编码器 / 冻结自监督编码器）
- L1 k-匿名分段统计、L2 粗粒度标记
- T-learner 增量模型
- ECDH-PSI 行为仿真 + 错配注入

---

## 二、降级清单

### 2.1 单步一提交 → 单模块一提交

- **原标准**：每个步骤单独提交，带完整步骤元数据与四维打分。
- **降级为**：按模块聚合提交。
- **原因**：初版冲刺涉及 5 个模块、约 20 个逻辑步骤；逐步提交的元数据开销
  会超过实现本身，且中间状态无法通过门禁（组件未成套）。
- **代偿**：changelog 中逐步记录 what/why/verification；四维打分仍按步骤给出。
- **恢复条件**：初版合入 main 后立即恢复。

### 2.2 差分隐私会计

- **原标准**：给出端到端 (ε, δ) 预算。
- **降级为**：只给**单轮**高斯机制 ε（灵敏度取 1）。
- **原因**：严格会计需 RDP + 子采样放大的实现与验证，工作量与建模主体相当。
- **风险**：单轮 ε 经 400 轮朴素合成后**不具备可解释的隐私含义**。
- **代偿**：结论文档 §5.2 明确标注「不可作为差分隐私保证引用」；
  且实验已证明该防护路线本身无效（跨轮平均攻击），**该降级不影响主结论**。
- **恢复条件**：若未来真的采用噪声类防护——但按 C3，不应采用。

### 2.3 密码学原语

- **原标准**：实现或集成 PSI / 同态加密 / 秘密分享。
- **降级为**：PSI 用「带噪声的确定性匹配」仿真，只保证**下游影响**可信。
- **原因**：密码学实现的正确性验证需要独立的安全评审，本项目不具备该条件。
- **代偿**：明确标注为仿真；成本估算按 ECDH-PSI 的公开量级给出，用于可行性判断而非性能承诺。
- **恢复条件**：进入工程化阶段（M8）前必须换为真实实现或成熟库。

### 2.4 M4 特征工程

- **原标准**：完整特征工程链路（分箱、WOE、稳定性筛选、特征库）。
- **降级为**：不单独建模块，特征由 SCM 直接生成。
- **原因**：合成数据的特征已是干净的连续变量，特征工程无从发挥；
  在合成数据上做特征工程只会产生**看似完整实则无信息量**的产物。
- **风险**：真实数据上特征工程可能显著改变各级别的相对表现。
- **代偿**：列入结论文档 §9 已知缺口第 5 项。
- **恢复条件**：接入真实数据时必须补齐。

### 2.5 M8 工业化 / M9 交付

- **原标准**：可运行的服务化链路 + 完整交付包。
- **降级为**：文档级说明，无物理交付物。
- **原因**：无真实数据与真实双方环境，服务化无对象。
- **恢复条件**：M0 的合规路径确定且数据到位后。

### 2.6 M0 合规的 DoD

- **原标准**：跨境对象定性经法务复核。
- **降级为**：`self_assessed_no_legal_review`（豁免 W-001）。
- **原因**：本项目不设合规角色，由用户明确决定。
- **代偿**：所有涉法结论带 `mandatory_disclaimer`；从不标注为 `done`。

---

## 三、**未**降级的项（红线）

以下为 DR-GOV-009 与 W-002 明确的不可降级项，本次全部遵守：

| 红线 | 遵守情况 |
|---|---|
| 所有实验 ≥5 种子 | ✅ 主网格 5 种子；关键对照 **40 种子** |
| 主指标带置信区间 | ✅ 全部 AUC 与差值均给跨种子 95% CI |
| L1 基线必须与 L3 同等认真实现 | ✅ 见 §四 |
| 数据分级与 0.7 红线 | ✅ 未触碰 |
| 禁止事项（数据类 / 科学性类） | ✅ 未违反 |
| 合成数据不得用于承诺真实效果 | ✅ 每份产出首部均有边界声明 |

---

## 四、L1 基线的实现强度自证

DR-GOV-009 规定「**弱化 L1 等于伪造结论**」。为自证未弱化，本次做了三重加强并全部报告：

1. **分段数扫描** 16 / 64 / 256 / 1024——取最有利的配置报告
2. **k 值扫描** 5 / 10 / 20——确认 k-匿名强度不是瓶颈（AUC 差异 < 0.0001）
3. **安慰剂对照**——把 B 侧换成纯噪声，分离出「分段本身的灵活性」与「B 的真实贡献」，
   避免把前者误算为 L1 的功劳（若不做此对照，会**高估** L1，
   即该设计是**对 L1 有利**方向的严格化）
4. **关键对照单独用 40 种子**，而非 5 种子

结论 `L1 捕获 VFL 价值的 12.3%` 是在上述最有利于 L1 的条件下得出的。

同时报告了 L1 的**结构性上界**：分段键只能用主动方特征，
故回传统计量是主动方特征的函数，信息量在很大程度上已被主动方特征包含。
这是 L1 的机制限制，不是实现缺陷。

---

## 五、绕开的强制要求及说明

用户指令允许「遇到强制性要求阻拦推进时适当绕开，但必须说明」。本次绕开两处：

1. **门禁 5（代码质量）与门禁 7（端到端冒烟）尚未实现**
   - 现状：这两个门禁在治理骨架阶段就标记为「待第一份代码组件 / 主链路出现」。
   - 本次首次产生了成套代码组件与主链路，**触发条件已满足**，但未实现。
   - 说明：实现这两个门禁需再一轮治理层工作，会阻断实验交付。
   - **列为初版合入后的第一优先项。**

2. **多路线分支卡（branch card）未逐路线建立**
   - 现状：L3 的三条路线（联邦LR / 纵向GBDT / SplitNN）在同一提交中并行实现。
   - 原因：三者共享数据生成与评估链路，拆分为三张分支卡会导致大量重复。
   - 代偿：三条路线的结果**分别独立报告**，未做择优隐藏。

---

## 六、下一步建议（按优先级）

1. 实现门禁 5 与门禁 7（触发条件已满足）
2. 恢复单步一提交
3. 补齐特征推断与成员推断攻击（结论文档 §9 第 4 项，最可能改变结论）
4. 各级别做超参搜索，检验 C1 的稳健性
5. 换用第二种 SCM 结构，检验结论对生成假设的稳健性
6. 待用户提供真实资格率 / 双持率后，重估 N_eff 与 M0 出路选择
