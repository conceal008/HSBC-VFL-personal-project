# 交接记录：项目框架与进度统一

> 会话日期：2026-09-16 至 2026-09-17 ｜ Agent：Codex ｜ 范围：GOV
> Step-Id：S-GOV.11 ｜ Change-Id：CL-20260916-GOV-001

## 本次完成

| 步骤 | 分数 | 状态 | 产出 |
|---|---:|---|---|
| S-GOV.11 统一项目目标、规划方向与进度口径 | 11/12 | committed | 根目录《项目框架与推进总览》、入口提示、步骤声明、changelog 与本交接 |
| S-INIT.9 修复门禁在 macOS Bash 3.2 下的变量边界 | 11/12 | committed | 两份门禁脚本、步骤声明、changelog；回归 47/47 |

本次没有新增实验、算法、法律判断或业务结论。主要工作是把三个时间层的材料分开：8–9 月规范用于定义完整框架，根目录材料用于提炼已有结论，模块台账与实际产物用于判断当前成熟度。

## 核对结论

- 修改前十份模块台账合计记录 26 条步骤，条目数与各台账 `steps_used` 一致；该数字只代表记录量，不是项目完成率。
- M1 仍无正式步骤；M2/M3/M5/M6/M7 已形成不同程度的合成数据或仿真产物；M4 是规格级；M8 是原型鲁棒性级；M9 是文档与机器核验级；M0 已上报但未完成最后裁定。
- S7.4、S7.5 已覆盖部分恶意行为、模型窃取与多次建模累积，旧清单里的“恶意参与方未评估”已经过时；合谋和更强偏离仍未闭合。
- README 初始化状态、旧进度快照和部分模块 README 存在滞后。为遵守历史只加不删，本次用统一入口和更新提示处理，没有批量改写历史正文。

## 验证

- `git diff --check`：通过。
- 门禁 1：当前仓库 `BLOCK=0, WARN=0`。
- changelog schema、合规一致性、可复现性、端到端冒烟、M9 证据链：通过；证据链为 17/17、10/10、10/10、3/3。
- 代码质量：沙箱外运行，147 个测试通过，核心逻辑行覆盖 97.3%，其余检查通过。
- 门禁回归套件初次为 42/47；定位到 macOS Bash 3.2 的变量边界问题后，以独立步骤 S-INIT.9 修复，最终 **47/47**。
- 本次没有重新运行 11 份实验 notebook，也没有验证仓库外的数据下载状态。

## 下一步

1. P0：做 M1 候选数据的案头评估、许可核对与数据卡。
2. P0：向需求方收敛产品、人群、标签、窗口、资格率、双持率与同意率，形成假设确认单。
3. P1：数据就绪后按四种切分协议运行 M4→M5→M6，先 L0/L1，再 L3。
4. P1：按最新 M7 结果重写安全差距和上线阻塞，立项真实 PSI 与协议级标签保护。

## 试过但没有继续的事

- 先在受限沙箱运行全量门禁，notebook 构建测试因不能绑定本机临时端口失败；沙箱外复跑后 147 个测试全部通过。
- 以 `C.UTF-8`、`en_US.UTF-8`、`zh_CN.UTF-8` 复跑，确认失败不由 locale 选择造成；最小复现表明 `$f（` 会丢失变量值。随后另立 S-INIT.9，用 `${f}` 明确变量边界并修复。

## 接手提醒

先读根目录《项目框架与推进总览》。不要把 26/103 写成完成率，不要把合成数据结论写成真实营销效果，不要把机器证据核验写成法务签署或生产验收。
