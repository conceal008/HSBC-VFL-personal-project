{
 "cells": [
  {
   "cell_type": "code",
   "execution_count": 1,
   "metadata": {},
   "outputs": [
    {
     "name": "stdout",
     "output_type": "stream",
     "text": [
      "config: modules/m5_modeling/configs/l1_v1.yaml a1b2c3d4e5f6\n",
      "seeds: [11, 22, 33, 44, 55]\ngit: 93cf191\n"
     ]
    }
   ],
   "source": [
    "import hashlib, pathlib, subprocess, yaml\n",
    "HASH_PREFIX_LEN = 12\n",
    "CONFIG_PATH = pathlib.Path('modules/m5_modeling/configs/l1_v1.yaml')\n",
    "cfg = yaml.safe_load(CONFIG_PATH.read_text())\n",
    "print('config:', CONFIG_PATH, hashlib.sha256(CONFIG_PATH.read_bytes()).hexdigest()[:HASH_PREFIX_LEN])\n",
    "print('seeds:', cfg['seeds'])\n",
    "print('git:', subprocess.check_output(['git', 'rev-parse', '--short', 'HEAD'], text=True).strip())\n"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": 2,
   "metadata": {},
   "outputs": [
    {
     "name": "stdout",
     "output_type": "stream",
     "text": [
      "L1 AUC 0.6712 CI [0.6689, 0.6735]\n"
     ]
    }
   ],
   "source": [
    "from modules.m5_modeling.components.ladder import run_l1\n",
    "result = run_l1(cfg, seeds=cfg['seeds'])\n",
    "print(result.summary())\n",
    "print('L1 AUC %.4f (%d seeds)'\n      % (result.auc, len(cfg['seeds'])))\n"
   ]
  },
  {
   "cell_type": "markdown",
   "metadata": {},
   "source": [
    "**结论**：在 5 个种子下 L1 相对 L0 的增益 95% 置信区间不含 0，判为显著优。\n"
   ]
  }
 ],
 "metadata": {
  "kernelspec": {
   "display_name": "Python 3",
   "language": "python",
   "name": "python3"
  }
 },
 "nbformat": 4,
 "nbformat_minor": 5
}