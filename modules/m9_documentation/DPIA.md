# M9 · 数据保护影响评估（DPIA）

- 产出日期：2026-09-04（步骤 S9.1）
- 状态：🔴 **草案，未经法务复核**——本项目不设合规角色（豁免 W-001）
- 评估对象：纵向联邦学习在南向通首次申购场景的应用

> ⚠️ **本文件不构成合规意见。** 它的作用是把技术侧实测到的风险按 DPIA 的结构整理出来，
> 供未来的合规责任人接手，而不是替代那个人。

---

## 一、处理活动概述

| 项 | 内容 |
|---|---|
| 目的 | 提升南向通首次申购的营销名单质量 |
| 主动方 | 内地行（持有标签与内地侧特征） |
| 被动方 | 香港行（持有香港侧特征） |
| 数据主体 | 双持客户（同时是两方客户） |
| 处理方式 | 纵向联邦建模，双方不交换原始特征 |
| 跨境情形 | 存在，八类对象定性见 `registry/cross_border_assets.yaml` |

---

## 二、风险登记（每条溯源到具体实验）

| 编号 | 风险 | 严重度 | 缓解措施 | 实验依据 |
|---|---|---|---|---|
| **R1** | 被动方原始特征被主动方精确恢复 | **高** | 上行加噪 σ ≥ 0.1（实测 R² 1.0000→0.196，可用性损失 0.0014） | `m7/results/feature_inference.csv` |
| **R2** | 主动方标签被被动方完全推断 | **高** | **无有效防护**——噪声已证伪，须协议级手段，本阶段未实现 | `m7/results/multiround_attack.csv` |
| R3 | 成员关系泄露 | 中 | 控模型容量，GBDT 树深 ≤4 | `m7/results/membership_lira.csv` |
| R4 | SplitNN 形态B 特征反演风险高于形态A | 中 | 形态B 须配自监督预训练；两类暴露面分别评估 | `m7/results/attack_results.csv` |
| R5 | 实体错配导致个人信息被错误关联 | 中 | 错配率监控；合规上错配本身即是事故 | `m3/results/misattribution_impact.csv` |
| R6 | 静默降级导致下游误用单方模型名单 | 中 | 降级必须上报并告警；故障注入 F2 已验证 | `m8/components/fault_injection.py` |
| R7 | schema 变更后照常出分，名单全错 | **高** | schema 守卫停止出分，不得被重试掩盖；F4 已验证 | `m8/components/fault_injection.py` |
| **R12** | 恶意主动方用构造探针直接读出被动方原始特征 | **高** | 上行加噪 **与** 残差合法性检查必须同时开启——两者缺一即失效 | `m7/results/malicious_probe.csv` |
| **R13** | 恶意被动方定向抬分操纵营销名单 | **高** | 名单稳定性阈值收紧至 0.95（仅检测）；根治需可验证的部分 logit，未实现 | `m7/results/malicious_boost.csv` |
| **R14** | 被动方窃取主动方模型，独立复现整份名单（商业资产直接转移） | **高** | 下行加噪 σ≥1.0——防不住标签但能把名单复现度由 1.000 压到 0.13，可用性代价近零 | `m7/results/model_stealing.csv` |

**风险溯源率：10/10 = 100%**（每条都指向具体的实验产出，由 `evidence_chain.py` 机器核验）

---

## 三、剩余风险

**R2（标签完全泄露）目前无有效技术缓解。**

这是本评估最重要的结论：噪声类防护已被实测证伪——把泄露压到 0.67 需要 σ=30，
而那时模型可用性（0.6352）已低于不做联邦的内地单方基线（0.7089）。
**防护到有效时，还不如不做联邦。**

→ 在安全聚合 / 同态加密 / 秘密分享落地之前，
**涉及标签跨方交换的路线（L3a、L3c 形态A）不应进入生产**。

---

## 四、本评估的局限

1. 全部实验在**合成数据**上完成，阈值接入真实数据后必须重新标定
2. 攻击者设定为**半诚实 + 部分恶意**；S7.4 已覆盖恶意主动方探针与恶意被动方抬分，
   但**仍未覆盖合谋、模型窃取、攻击者篡改自身模型结构**
3. 未覆盖模型窃取攻击、针对纵向 GBDT 的专门特征推断
4. A5 成员推断用 32 个影子模型（LiRA 原文通常 64–256），数值为**下界**
5. **未经法务复核**——本项目不设合规角色

**R12 与 R13 是 S7.4 新增的，且它们补上了本项目自身防护基线的一个漏洞**：
此前 M8 只要求上行加噪，没要求残差合法性检查——而实测证明**噪声单独无效**。

第 2 项与第 5 项仍是最可能改变本评估结论的两项。
